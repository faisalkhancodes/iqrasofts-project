{
  "name": "iqrasofts",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix ai-service",
    "dev": "concurrently -n server,client,ai \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"npm run dev --prefix ai-service\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
